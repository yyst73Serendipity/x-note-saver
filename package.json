{
  "name": "x-note-saver",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "test:integration": "firebase emulators:exec --project demo-x-note-saver --only firestore \"node --test tests/integration/*.test.js\"",
    "deploy": "npm run build && firebase deploy --project x-note-saver --only firestore:rules,hosting"
  },
  "dependencies": {
    "firebase": "^12.19.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "esbuild": "^0.28.2",
    "firebase-tools": "^15.30.2"
  }
}
